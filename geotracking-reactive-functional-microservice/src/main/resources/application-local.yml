spring:
  config:
    activate:
      on-profile: "local"

  data:
    mongodb:
      uri: mongodb://localhost:27017/demo_webflux?retryWrites=true&w=majority&maxPoolSize=100&maxIdleTimeMS=120000

      # https://www.mongodb.com/docs/manual/administration/connection-pool-overview/#:~:text=mongos%20routers%20have%20connection%20pools%20for%20each%20node%20in%20the%20cluster.

logging:
  level:
    #root: DEBUG
    org.springframework.data.mongodb.core.ReactiveMongoTemplate: DEBUG     #DEBUG #ERROR
    org.springframework.data.mongodb.repository.query: DEBUG       #DEBUG #ERROR


